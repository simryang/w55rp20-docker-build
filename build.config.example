# W55RP20-S2E 빌드 로컬 설정 (선택적)
#
# 사용법:
#   cp build.config.example build.config
#   vim build.config  # 아래 항목 수정
#
# build.config는 .gitignore되므로 git diff에 나타나지 않습니다.
# 대부분 경우 이 파일 없이 기본값만으로 충분합니다.
#
# 고성능 머신이나 저사양 환경(라즈베리파이 등)에서만
# 커스터마이징하세요.

# ============================================================================
# 빌드 병렬도 (기본: 16)
# ============================================================================
# - CPU 코어 수에 맞춰 조정
# - 고성능 워크스테이션 (32코어): 32
# - 일반 데스크탑 (8코어): 8
# - 라즈베리파이 (4코어): 4
#
#JOBS=16

# ============================================================================
# RAM 디스크 크기 (기본: 24g)
# ============================================================================
# - tmpfs는 실제 사용량만 메모리를 차지합니다 (limit은 상한선)
# - 실제 빌드 사용량은 ~8GB 정도
# - 64GB RAM 시스템: 48g
# - 32GB RAM 시스템: 24g (기본값)
# - 16GB RAM 시스템: 12g
# - 8GB RAM 시스템 (라즈베리파이): 4g
#
#TMPFS_SIZE=24g

# ============================================================================
# 고급 옵션 (일반적으로 건드릴 필요 없음)
# ============================================================================

# 이미지 자동 빌드 (기본: 1)
# - 1: 이미지 없으면 자동 빌드 (권장)
# - 0: 이미지 없으면 에러 (고급 사용자용)
#AUTO_BUILD_IMAGE=1

# 소스 자동 갱신 (기본: 0)
# - 0: 기존 소스 사용 (재현 가능한 빌드)
# - 1: git fetch + checkout (항상 최신 코드)
#UPDATE_REPO=0

# 산출물 정리 (기본: 0)
# - 0: 기존 산출물 유지
# - 1: *.uf2, *.elf, *.bin 삭제 후 빌드
#CLEAN=0

# 빌드 타입 (기본: Release)
# - Release: 최적화, 배포용
# - Debug: 디버그 심볼 포함
#BUILD_TYPE=Release

# Docker 이미지 이름 (기본: w55rp20:auto)
#IMAGE=w55rp20:auto

# Docker 플랫폼 (기본: linux/amd64)
# - linux/amd64: x86_64 (일반 PC)
# - linux/arm64: aarch64 (라즈베리파이 등)
#PLATFORM=linux/amd64
